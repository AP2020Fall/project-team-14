package model;

public class Admin extends Account{
    public Admin(String firstName, String lastName, String username, String userId, String password, String phoneNumber, String emailAddress) {
        super(firstName, lastName, username, userId, password, phoneNumber, emailAddress);
    }
}
